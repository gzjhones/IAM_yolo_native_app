name: yolo_native_app
description: YOLO Object Detection con código nativo

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  permission_handler: ^11.0.1
  image: ^4.0.17
  http: ^1.1.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/services_config.json